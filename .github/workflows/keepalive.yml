name: Streamlit Keep Alive Bot

on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: |
          curl -I https://pm10-research-app-pm2hpzsoukclndgohgwoac.streamlit.app
